# CASP
CASP (Coverage Approximation via Static Profiles) approximates code coverage without execution by using LLVM Block Frequency Information (BFI) to map static execution frequencies to program counters and emit LLVM-compatible .profdata, enabling coverage estimation via existing LLVM tools.
